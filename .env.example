# Pi Brain environment configuration
# Copy this file to .env and adjust values for your environment.
# The start_dev.sh script loads this automatically.

# Directory where Pi Brain stores its runtime config file (main.cfg).
# Defaults to ~/.pi_brain/ if not set.
# PI_BRAIN_CONFIG_DIR=/home/pi/.pi_brain/
